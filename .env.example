# CLIENT ONLY: the React app calls this API base URL.
# Restart npm run dev after changing this file.
# The in-app "Change API URL" button can override this, but changing this value invalidates old overrides.
VITE_API_BASE_URL=http://localhost:3000
